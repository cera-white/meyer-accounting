@model IEnumerable<MeyerAccountingV2.EF.StaffQualification>

@{
    ViewBag.Title = "Manage Qualifications";
    ViewBag.FooterInfo = "<a href='@Url.Action('Contact', 'Home')'>Contact</a>";
}

<div class="container">
    <div class="row pt-xl mb-lg">
        <div class="col-md-12">
            <h1 class="mt-xl mb-none">@ViewBag.Title for @ViewBag.Staff.FullName</h1>
            <div class="divider divider-primary divider-small mb-xl">
                <hr>
            </div>

            <p>
                @Html.ActionLink("Create New", "Create", new { id = ViewBag.Id })
            </p>
            <table class="table">
                <tr>
                    <th>
                        Staff Member
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Description)
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Staff.FullName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Description)
                        </td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.QualificationId }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.QualificationId })
                        </td>
                    </tr>
                }

            </table>

        </div>
    </div>
</div>



