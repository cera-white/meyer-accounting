@model MeyerAccountingV2.EF.StaffQualification

@{
    ViewBag.Title = "Delete Staff Qualification";
    ViewBag.FooterInfo = "<a href='@Url.Action('Contact', 'Home')'>Contact</a>";
}

<div class="container">
    <div class="row pt-xl mb-lg">
        <div class="col-md-12">
            <h1 class="mt-xl mb-none">@ViewBag.Title</h1>
            <div class="divider divider-primary divider-small mb-xl">
                <hr>
            </div>

            <div>
                <h4>Are you sure you want to delete this Staff Qualification?</h4>
                <hr />
                <dl class="dl-horizontal">
                    <dt>
                        Staff Member
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.Staff.FullName)
                    </dd>

                    <dt>
                        @Html.DisplayNameFor(model => model.Description)
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.Description)
                    </dd>

                </dl>

                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()

                    <div class="form-actions no-color">
                        <input type="submit" value="Delete" class="btn btn-default" /> |
                        @Html.ActionLink("Back to List", "Manage", new { id = Model.StaffId })
                    </div>
                }
            </div>

        </div>
    </div>
</div>



